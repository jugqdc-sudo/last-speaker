.env
.venv/
venv/
__pycache__/
*.pyc
.DS_Store
*.mp4
*.webm
*.mkv
*.log
data/*
runs/*/phones.json
*.wav
runs/*/log_reuse.txt

# internal notes, not for the public repo
CLAUDE.md

# tape metadata is code, not data: the site build needs it (dead timestamps without it)
!data/sources.json
